{% extends 'base.html.twig' %}

{% block title %}Contrats{% endblock %}

{% block body %}
<aside class=''>

		{% include "Templates_parts/aside.html.twig" %}
	</aside>
	<main>
		<nav id="menu_admin" class="mt-4">
			{% include "Templates_parts/_nav.html.twig" %}
		</nav>
		<hr>

			<a class="btn btn-secondary ml-2" href="{{ path('contrats_index') }}">
				<i class="fa fa-chevron-left"></i>
				Retour</a>
			<h2 class="mt-4 ml-4 d-flex justify-content-center text-light"> Contrat</h2>
		<section id="ajouter_technicien" style="background-color:white" class=" container-fluid">
 

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contrat.id }}</td>
            </tr>
            <tr>
                <th>TypeContrat</th>
                <td>{{ contrat.TypeContrat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('contrats_index') }}">back to list</a>

    <a href="{{ path('contrats_edit', {'id': contrat.id}) }}">edit</a>

    {{ include('contrats/_delete_form.html.twig') }}
    </section>
	</main>
{% endblock %}
