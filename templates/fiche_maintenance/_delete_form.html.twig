<form method="post" action="{{ path('fiche_maintenance_delete', {'id': fiche_maintenance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fiche_maintenance.id) }}">
    <button id="boutton_show" class="btn">Delete <i class="fa fa-trash"></i></button>
</form>
