{% extends 'base.html.twig' %}

{% block title %}Edit Clients
{% endblock %}

{% block body %}

	{% if utilisateur != null %}
		<aside class='mh-100'>
			{% if utilisateur.roles[0] == "ROLE_ADMIN" %}

				{% include "Templates_parts/aside.html.twig" %}

			{% elseif utilisateur.roles[0] == "ROLE_USER" %}
				{% include "Templates_parts/aside2.html.twig" %}
			{% endif %}

		</aside>
	<main>
	{% if utilisateur.roles[0] == "ROLE_ADMIN" %}
				<nav id="menu_admin" class="mt-3">


					{% include "Templates_parts/_nav.html.twig" %}


				</nav>
			{% else %}
				<nav style='height: 9vh;
													    width: 80vw;'></nav>
			{% endif %}
		
		<hr>
{% if utilisateur.roles[0] == "ROLE_ADMIN" %}
			<a class="btn btn-secondary ml-2" href="{{ path('clients_show', {'id': client.id}) }}">
				<i class="fa fa-chevron-left"></i>
				Retour</a>
				{% elseif utilisateur.roles[0] == "ROLE_USER" %}
<a class="btn btn-secondary ml-2" href="{{ path('profil_clients') }}">
				<i class="fa fa-chevron-left"></i>
				Retour</a>

				 {% endif %}
			<h2 class="mt-4 ml-4 d-flex justify-content-center text-light">Modifier le Client</h2>
		<section id="ajouter-technicien" style='background-color:white' class=" container-fluid">
		<br>
			<article class="mt-4 container-fluid"  >

				{{ include('clients/_form.html.twig', {'button_label': 'Enregistrer'}) }}

{% if utilisateur.roles[0] == "ROLE_ADMIN" %}

				{{ include('clients/_delete_form.html.twig') }}
{% endif %}
			</article>
		</section>
	</main>
	{% else %}

	{% endif %}
{% endblock %}
